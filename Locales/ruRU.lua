if not ((GAME_LOCALE or GetLocale()) == "ruRU") then
  return
end
local addonName, VT = ...
local L = VT.L
L = L or {}

--@localization(locale="ruRU", format="lua_additive_table", namespace="VT", handle-subnamespaces="none")@
