if not ((GAME_LOCALE or GetLocale()) == "ptBR") then
  return
end
local addonName, VT = ...
local L = VT.L
L = L or {}

--@localization(locale="ptBR", format="lua_additive_table", namespace="VT", handle-subnamespaces="none")@
